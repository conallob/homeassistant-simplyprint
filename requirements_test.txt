pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
aioresponses>=0.7.6
# homeassistant provides the HA library imports used by the integration;
# pytest-homeassistant-custom-component brings it in as a transitive dep
# but also loads fixtures that conflict with our own mocks, so we
# declare homeassistant directly and skip the custom-component plugin.
homeassistant>=2024.1.0
voluptuous>=0.13.1
